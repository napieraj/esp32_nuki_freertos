esphome:
  name: nuki-bridge

esp32:
  board: esp32-s3-devkitc-1
  framework:
    type: esp-idf

psram:
  mode: octal
  speed: 80MHz

wifi:
  ssid: "TestNetwork"
  password: "testpassword123"

api:

logger:

external_components:
  - source:
      type: local
      path: components

lock:
  - platform: nuki_pro
    name: "Front Door Nuki 5.0"
    pin: "065432"
